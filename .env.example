# API
API_KEY=change-me

# For the first run it's convenient to insert demo data automatically.
# Set to false in production.
SEED_DATA=true

# Postgres (docker-compose)
POSTGRES_DB=org_directory
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Local run (when you run uvicorn on host machine):
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/org_directory
#
# In docker-compose, DATABASE_URL is set automatically to host "db".
